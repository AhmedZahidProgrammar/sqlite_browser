import 'package:flutter/cupertino.dart';

class InsertRecordPage extends StatelessWidget {
  const InsertRecordPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [

      ],
    );
  }
}
